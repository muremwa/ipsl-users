import { CompanyHighlightDirective } from './company-highlight.directive';

describe('CompanyHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new CompanyHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
